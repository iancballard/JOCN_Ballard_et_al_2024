#color/motion coherences for each subject
coherences = dict(
	test = dict(color = .6,
                motion = .05,
                shape = .6),
    subj08 = dict(color = .59,
                motion = .08,
                shape = .67),
    )